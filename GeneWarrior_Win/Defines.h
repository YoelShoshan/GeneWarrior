#ifndef GW_DEFINES_H
#define GW_DEFINES_H

#pragma once

#define MOTORS_NUM 9

//////////////////////////////////////////////////////////////////////////////////////////////////////////////
// both OutputDebugString and log file
//#define ALLOC_LOG char log[1024*5];
//#define SPRINTF(...) sprintf(__VA_ARGS__)
//#define ODS(x) { OutputDebugStringA(x); fprintf(g_pLog,x);}
//////////////////////////////////////////////////////////////////////////////////////////////////////////////

//////////////////////////////////////////////////////////////////////////////////////////////////////////////
// only OutputDebugString
/*#define ALLOC_LOG char log[1024*5];
#define SPRINTF(...) sprintf(__VA_ARGS__)
#define ODS(x) { OutputDebugStringA(x);}
#define OUTPUT(...) { fprintf(g_pLog, __VA_ARGS__); fflush(g_pLog);}*/
//////////////////////////////////////////////////////////////////////////////////////////////////////////////

//////////////////////////////////////////////////////////////////////////////////////////////////////////////
// output only into file
#define ALLOC_LOG char log[1024*2];
#define SPRINTF(...) sprintf(__VA_ARGS__)
#define ODS(x) { fprintf(g_pLog,x); fflush(g_pLog);}
#define OUTPUT(...) { fprintf(g_pLog, __VA_ARGS__); fflush(g_pLog);}
//////////////////////////////////////////////////////////////////////////////////////////////////////////////


//////////////////////////////////////////////////////////////////////////////////////////////////////////////
// no output
/*#define ALLOC_LOG char log[1024*2];
#define SPRINTF(...) 
#define ODS(x) 
#define OUTPUT(...) */
//////////////////////////////////////////////////////////////////////////////////////////////////////////////





#endif